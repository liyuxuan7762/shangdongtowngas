{
  "config": {
    "step": {
      "user": {
        "title": "TownGas Meter — Get Auth Link",
        "description": "Scan with WeChat to authorize:\n\n{oauth_qr_markdown}\n\nOAuth URL: {oauth_url}",
        "data": {}
      },
      "auth": {
        "title": "TownGas Meter — Enter Auth Code",
        "description": "After scanning, paste the authCode from the redirect URL below.\n\n{oauth_qr_markdown}\n\nOAuth URL: {oauth_url}",
        "data": {
          "auth_code": "authCode (or full redirect URL)",
          "subs_id": "Subscription ID (subsId)",
          "subs_code": "Account Number (subsCode)",
          "scan_interval": "Data refresh interval (seconds, 60-86400, default 21600)",
          "token_refresh_interval": "Token refresh interval (seconds, 300-7140, default 1800)"
        }
      },
      "reauth_confirm": {
        "title": "TownGas Meter — Re-authenticate",
        "description": "Token has expired. Please scan with WeChat to re-authorize:\n\n{oauth_qr_markdown}\n\nOAuth URL: {oauth_url}",
        "data": {}
      },
      "reauth_token": {
        "title": "TownGas Meter — Enter New Auth Code",
        "description": "After scanning, paste the new authCode below.\n\n{oauth_qr_markdown}\n\nOAuth URL: {oauth_url}",
        "data": {
          "auth_code": "authCode (or full redirect URL)"
        }
      }
    },
    "abort": {
      "oauth_url_failed": "Failed to get WeChat OAuth URL. Please try again later.",
      "already_configured": "This account is already configured. Remove the existing entry first if you want to re-add it."
    },
    "error": {
      "exchange_failed": "Token exchange failed. Please check the authCode.",
      "auth_failed": "Authorization failed. No valid token received.",
      "subs_required": "Subscription ID and account number cannot be empty."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "TownGas Meter — Settings",
        "data": {
          "scan_interval": "Data refresh interval (seconds, 60-86400, default 21600)",
          "token_refresh_interval": "Token refresh interval (seconds, 300-7140, default 1800)"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "gas_meter_reading": {
        "name": "Gas Meter Reading"
      }
    },
    "button": {
      "refresh_data": {
        "name": "Refresh Now"
      }
    }
  }
}
